# Plan 11 — Remove proxy/webRequest + tab previews [M2]

> Part 11 of the Sidebery → Chromium MV3 migration.
> Master plan: `docs/migrate-plan.md` — read §4.3, §4.9 and decision #3 in §0.
> Prerequisites: Plans 1–8. (Plan 3 already excluded `popup.proxy` and the
> `injections/tab-preview` bundle from the build.)

## Goal

Stub the per-request proxy / webRequest services and remove the tab-previews feature
(no screenshot mode, no fallback) from the Chromium build.

## Ground rules

No upstream `src/` edits — overlays (identical export shapes) or `patches/`.
**Commit after each completed TODO** (`chromium: <summary>`). Firefox build/tests green.

## TODOs

1. **Proxy/webRequest stubs (§4.3).**
   - Overlay `src/services/web-req.bg.ts` (~400 loc upstream) with a stub exporting
     no-op `updateReqHandlers`, `checkIpInfo`, `disableAutoReopening`,
     `enableAutoReopening` — read the upstream file first and mirror ALL exports, not
     just these four.
   - Overlay `src/services/web-req.fg.ts` (~80 loc) similarly.
   - Grep `pageAction` call sites (4, proxy badge only) — already no-op'd in the shim
     (Plan 4); verify nothing else references the proxy popup page.
   - Commit.

2. **Tab previews removal (§4.9).**
   - Overlay `src/services/tabs.fg.preview.ts` with a stub: export shape preserved, all
     functions no-op. (This also removes the `code:`-injection call site flagged in
     Plan 5.)
   - Confirm `injections/tab-preview.ts` is not in the Chromium build inputs (Plan 3) and
     no Chromium bundle references it.
   - Hide the preview settings block in `settings.tabs.vue` — big file: use a patch in
     `patches/` adding `v-if="!IS_CHROMIUM"` around the preview block. NOTE: Plan 12
     also patches `settings.tabs.vue` (tab-hiding options). Coordinate: put both hunks
     in ONE patch file `patches/settings.tabs.vue.patch` — create it here with the
     preview hunk; Plan 12 extends it.
   - `windows.fg.ts` window screenshots (`selWinScreenshots`): feature-checked via
     `browser.tabs.captureTab` (undefined in the shim) — verify it stays inert by
     reading the code; hide the related setting (grep which settings `.vue` hosts it;
     patch/overlay).
   - `tabs.bg.ts` `tabsApiProxy`/`captureTab` paths: verify they're guarded (read the
     code); no change expected.
   - Commit.

3. **Verify.** Both builds green; Firefox tests pass. Grep `addon-chromium/` output for
   `captureTab\|tab-preview\|webRequest\|proxy.onRequest` — only inert stub remnants
   allowed. Commit fixes.

## Exit criteria

- webRequest/proxy and preview code stubbed out of the Chromium build; related settings
  hidden; no console-error-producing call paths remain.
