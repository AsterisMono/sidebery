# Plan 16 — Injections rework + discarded-tab creation emulation [M4]

> Part 16 of the Sidebery → Chromium MV3 migration.
> Master plan: `docs/migrate-plan.md` — read §5.4 (scripting) and §5.10 (discarded/title
> tab creation).
> Prerequisites: Plan 5 (`executeScript` shim with root-relative path fix; `code:`
> throws), Plan 11 (previews gone).

## Goal

Finish script-injection work: convert the last `code:` injection to `{func}`, rework the
group/url internal pages to load their scripts directly (Chrome forbids scripting
extension pages), and emulate `tabs.create({discarded: true, title})` via the
placeholder page + `tabs.discard`.

## Ground rules

No upstream `src/` edits — overlays (identical export shapes) or `patches/`.
**Commit after each completed TODO** (`chromium: <summary>`). Firefox build/tests green.

## TODOs

1. **Force-discard `code:` injection → `{func}`.**
   - Locate the `code:` call site in `src/services/tabs.fg.ts` (~line 1167; grep
     `executeScript` + `code`). Via targeted patch (file is huge) replace it with a
     Chromium branch calling `chrome.scripting.executeScript({ target, func, args })`.
     Verified: `func` is serialized (closures lost) — pass everything through
     JSON-serializable `args`. Alternatively add the helper to
     `src.chromium/platform/` and patch the call site to use it. Commit.

2. **Group/url page script loading (§5.4 last bullet).**
   - Upstream `tabs.bg.ts` `initInternalPageScripts` injects `sidebery/group.js` /
     `url.js` into the extension's own `group.html`/`url.html` tabs — Chrome forbids
     scripting extension pages.
   - Overlay the staged copies of `group.html`/`url.html` (find them: grep
     `group.html` under `src/` — likely `src/page.group/`): add `<script src>` tags
     loading their scripts directly. The pages fetch init data over IPC — the
     `getGroupPageInitData` action already exists (grep it to confirm the flow and adapt
     the page scripts' init via overlay if they currently expect injected globals).
   - Patch/overlay `tabs.bg.ts`'s `initInternalPageScripts` path to skip injection on
     Chromium (empty function via small patch, or overlay module).
   - Cross-check with Plan 7's color-scheme work: group-page init data must carry the
     scheme from the fg side.
   - Commit.

3. **Discarded-tab creation emulation (§5.10).**
   - Verified: Chrome `tabs.create()` accepts neither `discarded: true` nor `title`.
     Create the real URL without those Firefox-only properties, wait for Chrome's first committed
     URL, then call `chrome.tabs.discard(tabId)`. Discarding immediately after `tabs.create()` can
     produce an unloaded tab with no `url` or `pendingUrl`, leaving nothing to load on activation.
   - Create helper `src.chromium/platform/discarded-tabs.ts`:
     `createDiscardedTab(props)` → strips `discarded`/`title`, creates the real URL, waits for its
     first commit, then discards. Leave the tab loaded if no URL commits promptly.
   - Call sites to convert (overlay/patch each): `src/services/tabs.fg.create.ts`
     ~lines 251 (Alt-drag) and 484 (bulk paste/open); snapshot restore paths (grep
     `discarded: true` under `src/`); `src/services/windows.bg.ts:createWithTabs`.
   - Audit that tree restore preserves the target URL while Chrome reports the tab as discarded.
   - Commit per call-site group.

4. **Media injections sanity check.** `src/services/tabs.fg.media.ts` (7 `file:` sites)
   should now work through the Plan-5 shim (root-relative path normalization,
   `injectImmediately`). Read the call sites once to confirm no `code:` or other
   surprises remain. Commit if fixes needed.

5. **Verify.** Both builds green; Firefox tests pass. Append to
   `docs/plans/notes-verify-in-chrome.md`: Alt-drag link → unloaded tab appears with
   title, activates to real URL; bulk-open 20 links as unloaded → no page loads fire;
   snapshot restore produces unloaded tabs; group page renders with correct data;
   pause-media injection works on a YouTube tab. Commit.

## Exit criteria

- Zero `code:` injections; internal pages self-load scripts; discarded-tab creation
  works via real-URL creation + discard on all four flows.
