# Hide the Firefox-only tab preview controls. This patch is applied only to the
# Chromium staging tree. Plan 12 extends it with the tab-hiding settings hunk.
diff --git a/src/page.setup/components/settings.tabs.vue b/src/page.setup/components/settings.tabs.vue
--- a/src/page.setup/components/settings.tabs.vue
+++ b/src/page.setup/components/settings.tabs.vue
@@ -503,7 +503,7 @@
       :default="DEFAULT_SETTINGS.inheritCustomColor"
       @update:value="Settings.saveDebounced(150)")
 
-  .wrapper(ref="tabsPreviewEl")
+  .wrapper(v-if="false" ref="tabsPreviewEl")
     .sub-title: .text {{translate('settings.nav_settings_tabs_preview')}}
     ToggleField.-no-separator(
       label="settings.tabs.preview"
