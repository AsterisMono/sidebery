# Missing session metadata is normal when Chromium has no matching local restart cache.
# Keep a low-noise diagnostic, then use the fallback panel without an undefined-panel warning.
diff --git a/src/services/tabs.fg.ts b/src/services/tabs.fg.ts
--- a/src/services/tabs.fg.ts
+++ b/src/services/tabs.fg.ts
@@ -516,5 +516,5 @@ function restoreTab(
     if (props.customTitle) tab.customTitle = props.customTitle
     if (props.customColor) tab.reactive.customColor = tab.customColor = props.customColor
   } else {
-    Logs.warn(`Tabs.restoreTab: no props for: "${tab.id} i${tab.index} url${tab.url}"`)
+    Logs.info(`Tabs.restoreTab: no props for: "${tab.id} i${tab.index} url${tab.url}"`)
   }
@@ -537,5 +535,7 @@ function restoreTab(
     }
   } else {
     // Panel is not found
-    Logs.warn(`Tabs.restoreTab: cannot find panel: "${props?.panelId}"; tab index: ${tab.index}`)
+    if (props) {
+      Logs.warn(`Tabs.restoreTab: cannot find panel: "${props.panelId}"; tab index: ${tab.index}`)
+    }
     const parentTab = Tabs.byId[tab.parentId]
